# Case Study NN — *<working title>*

### <One-sentence hook: what was audited/built, with what, and the surprising takeaway.>

> **DATA STATUS: ILLUSTRATIVE / VERIFIED** *(pick one; illustrative requires the badge until batch runs replace every number — see the internal data plan).*
>
> **License:** CC BY 4.0 (text and aggregated data). Raw images are never mirrored in this repository.

---

## 1. TL;DR

- 3–5 bullets: dataset size, headline result (gate distribution shift), the one standalone insight, the one-line bridge.
- Every bullet must be true with TileSmith removed from the sentence.

## 2. Why this study matters (even without TileSmith)

- The *reader's* problem, stated first. No product in the first paragraph — ever.
- Which known gap in the workflow this study illuminates.

## 3. Method

| Step | Detail |
|---|---|
| **Selection** | criteria fixed *before* scoring; document thresholds |
| **Sample** | counts, tile classes (16×16 pixel-art / 64×64-normalized HD / other) |
| **Scoring** | Runner A, six metrics (Seam · Border · Artifact · Pattern · Fidelity · Textile), gates ≥ 92 / 78–<92 / < 78 |
| **Fix pass** | Runner B / Doctor operation set used, ordering constraints, degradation penalties |
| **Verification** | "after" tiles re-scored cold; no gate re-tuning between phases |
| **Aggregation** | mean per tile → gate counts → pooled; multi-defect counting rules |

## 4. Findings

### 4.1 Gate distribution — before vs. after
(table: Production / Review / Reject counts and %, average score — same shape as CS-01)

### 4.2 Defect taxonomy for this dataset
(table: defect class → metric → prevalence → in-game symptom; add dataset-specific classes honestly)

### 4.3 Per-source view
(table: one row per pack/source; pseudonymize until license review clears naming)

### 4.5 Pre-registered correlation (if hypotheses exist)
(ρ + permutation p + exceptions; registered before the run; soft-form headlines only)

### 4.4 The delivery-format audit (Gap 0: atomization)
(atlas-only vs mixed vs loose; % of tiles locked inside sheets; grid-aligned vs packed — generated by `scripts/audit-delivery-format.ts`; include the "why do creators ship atlases" interpretation, clearly labeled as interpretation)

### 4.x "Three surprises worth knowing"
(numbered list — the shareable part; craft these carefully, they become the post headlines)

## 5. Three tiles, three journeys

One vignette per interesting defect class, each:

- **Before: score (gate)** — worst metric, what it looks like in-game
- **Fix:** which pipeline step, what it does *not* do (honesty beat)
- **After: score (gate)** — and how it was independently verified

## 6. The N-point checklist before you <ship/use/accept> <dataset-type>

Tool-agnostic value. Must be executable with zero TileSmith products.

## 7. Reproduce this study & score your own assets

```bash
# benchmark commands
```
```yaml
# TileSmith QC in CI (6 lines)
```

## 8. From measuring to fixing — the honest bridge

- One paragraph, max. Scoring = *that* it's broken; Doctor = *how* it gets ready. Local-first, no account to start. The loop closes in the reader's own CI.
- Never the opening line; always earned.

## 9. Limitations & data provenance

| Field | Value |
|---|---|
| Data status | ILLUSTRATIVE / VERIFIED |
| Sample size | … |
| Selection criteria | … |
| Dataset version | benchmark-vX fixture set … |
| Pipeline version | `tilefix-core` submodule commit … |
| Aggregation logic | … |
| Raw images | never mirrored in this repository |

## 10. Credits & license

CC BY 4.0 text/data · CC0 fixtures (Kenney, license file) · sources in `case-studies/NN/SOURCES.md` · links: benchmark · QC action · Studio.

---

**Editorial rules (keep in the template file):**

1. Standalone-value test: the study teaches something usable without our product.
2. Illustrative numbers are always visibly marked; empty aggregates are never presented as statistics.
3. One forward link per section, soft; no upsell language, no urgency, no superlatives.
4. The Doctor appears as the answer to a question the reader already has.
5. Same provenance block in every study; VERIFIED requires dataset version + core commit + aggregation logic pinned.
6. Include the delivery-format audit (Gap 0): atlas-only share of packs and % of tiles locked in sheets — atomization precedes quality.
7. **Frame the user, never the creator:** readiness ≠ quality; a Reject tile is not bad art; the story is the expectation gap (expert assets presuppose expert workflows — the mechanical half is automatable).
